# Aliyun npm registry (primary)
registry=https://registry.npmmirror.com/

# Fallback to official registry if Aliyun fails
# registry=https://registry.npmjs.org/

# Optional: Use Taobao registry (alternative to Aliyun)
# registry=https://registry.npmmirror.com/

# For specific package managers
# pnpm:registry=https://registry.npmmirror.com/
# yarn:registry=https://registry.npmmirror.com/

# Disable package lock for pnpm
package-lock=false

# Optional: Set for specific scopes
# @myscope:registry=https://registry.npmmirror.com/

# Optional: Set for specific packages with known issues
# puppeteer_download_host=https://npmmirror.com/mirrors/puppeteer